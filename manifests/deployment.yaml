apiVersion: apps/v1
kind: Deployment
metadata:
  name: pp-front:img42
  labels:
    app: pp-front:img42
  namespace: apps
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pp-front:img42
  template:
    metadata:
      labels:
        app: pp-front:img42
    spec:
      containers:
      - name: pp-front:img42
        image: 905418180391.dkr.ecr.us-east-1.amazonaws.com/pp-front:img42
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
      volumes:
      - name: data-vol
        persistentVolumeClaim:
          claimName: pp-front:img42
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: pp-front:img42
  labels:
    app: pp-front:img42
  namespace: apps
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  type: ClusterIP
  selector:
    app: pp-front:img42
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000